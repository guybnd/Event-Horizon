**REVIEW SYNTHESIS — FLUX-625 (external selector store)**

**Verdict: APPROVED — moving to Ready.** Three independent review passes (foundation by the lead; hot-path and cold-path migrations by parallel delegates) found **no Blocker and no Major** issues. The implementation is merged on master via PR #24 (commit `21c47a1`); the stale "uncommitted in worktree" notes no longer apply — the work landed.

Note: the `event-horizon` MCP tools were unavailable this session (server failed to load); this review used the read-only filesystem + the sanctioned REST fallback for ticket updates.

**What was reviewed**
- **Foundation** — `portal/src/store/appStore.ts` + `useAppSelector.ts` + the `AppContext.tsx` rewire. External store with shallow-diff `setState` (only notifies on real per-field changes); `useAppSelector` built on the official `useSyncExternalStoreWithSelector` shim with a stable `getSnapshot` (no "getSnapshot should be cached"/tearing hazard); `shallowEqual` helper for object selectors; actions exposed as a frozen, identity-stable set via a `latest`-ref delegation so action-only consumers never re-render; first-render store seed is guarded and fires before any subscriber exists. Correct.
- **Hot path** — Board, Column, `useTaskCardController`, `useTaskModalController`, BacklogScreen. Per-card granular selectors (`hasWorktree`, `worktreeChangedFiles`, `readComments[id]`, modal-open, `resolvedSubtasks`); the one object-returning selector (`resolvedSubtasks`) correctly passes `shallowEqual`; the `taskById`→`subtaskById` narrowing verified safe against its sole consumer (`CardSubtaskPopover`). Meets the per-card re-render goal by design.
- **Cold path** — all 32 migrated consumers are faithful mechanical splits of the old `useApp()` destructure into `useAppSelector` (state) + `useAppActions` (actions). No dropped keys, no wrong-field mappings, no unmemoized object/array selectors, imports correct.

**Acceptance criteria**
- ✅ `npm run check` passes (tsc, 0 errors — verified this session).
- ✅ All `useApp()` consumers migrated; the shim + old context value removed (no `useApp` references remain).
- ✅ No tearing / no getSnapshot-caching warning; object-returning selectors use shallow equality.
- ✅ Action-only consumers don't re-render on data updates (frozen action set).
- ✅ Per-card selectors achieve "a card re-renders only when its own task/slice changes" — *by construction*.
- ⏳ The runtime DevTools-Profiler confirmation (per-card commits while typing in search / on the 3s idle poll / during a live agent session; action-only no-rerender; dense-column drag smoothness, which absorbs FLUX-369) is **observational and requires running the app** — it's the natural Ready-stage review pass, not a code defect.

**Action items**
- Blockers: none.
- Major: none.
- Minor / forward-looking (no follow-up ticket filed — owned by the named tickets): `liveSessions` is mirrored as a frozen-empty constant (`EMPTY_LIVE_SESSIONS`) with **zero consumers today** — intentional scaffolding for FLUX-626. ⚠️ FLUX-626 must NOT simply add `liveSessions` to the AppContext snapshot, because the `useLayoutEffect` mirrors the snapshot on every commit and would clobber any SSE-`patch`ed live-session state back to empty; FLUX-626 should populate the slice via `appStore.patch` and drop it from the provider snapshot (or merge rather than overwrite). Flagging here so it isn't a surprise.

**Recommended Ready-stage check for the reviewer:** open React DevTools Profiler and confirm the three runtime scenarios above before promoting to Done (per the review-before-Done convention).
