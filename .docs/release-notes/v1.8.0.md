---
title: Release v1.8.0
---
## Release v1.8.0
*Released at 2026-07-19T12:51:43.419Z*

### Highlights

**True multi-workspace.** The headline of this release: one engine now serves N boards at once. Every layer went per-workspace — the workspace registry and live-board bootstrap (FLUX-1446, FLUX-1529), per-request HTTP/SSE routing via `X-EH-Workspace` (FLUX-1530), sync workers, write locks, timers, Furnace batches, notifications and HITL prompts (FLUX-1453, FLUX-1451, FLUX-1452, FLUX-1513, FLUX-1555), and CLI sessions tagged with their owning board (FLUX-1531). The portal switches boards non-destructively from a workspace tab strip — no reload, no killed sessions (FLUX-1454, FLUX-1455). Background loops (Temper, PR reconcile, gate runner) and engine self-dispatch now bind to the workspace that owns the work instead of whichever board was last opened (FLUX-1548, FLUX-1551, FLUX-1554), and the board orchestrator + Smelter chat each get a persistent session per workspace — switch, don't reset (FLUX-1580).

**Boot & runtime performance.** Cold boot went from ~60s on a 2000-ticket board to seconds via a parallel scan, persistent index, and two-phase load (FLUX-1547, FLUX-1540). The idle board no longer burns ~24 main-thread frames/sec (FLUX-1552), motion-batch re-render amplification is fixed (FLUX-1542), and interaction-burst render costs came down across cluster cards and token pills (FLUX-1553, FLUX-1549).

**Apple-feel polish, wave 2.** Ten polish rooms from the responsiveness audit (FLUX-1516): shared motion physics tokens with card→modal morph and card flight (FLUX-1507), optimistic commit-first actions with patch-first SSE (FLUX-1505), ghost session placeholders and a skeleton system over spinners (FLUX-1506), depth-stacked modals (FLUX-1518), staggered cold-boot reveal (FLUX-1519), rolling counters (FLUX-1520), toasts that fly from and dismiss into their card (FLUX-1522), interruptible retargetable springs (FLUX-1523), preserved scroll/selection across views (FLUX-1524), drag lift physics (FLUX-1525), and one success choreography — confetti is reserved for releases (FLUX-1526).

**Cheaper, smarter orchestration.** Batch grooming lets one session groom up to five sibling tickets in a sitting (FLUX-1383), each phase's dispatched lead now carries a deliberately trimmed toolset (FLUX-1462), the Scratchpad persona gained phase handoff and per-phase model overrides (FLUX-1479), and a communication-style contract makes agent output concise and reader-first across personas and skill modules (FLUX-1502). A new token observability panel surfaces agent-payload metrics and waste signals — including a digest-savings counterfactual and a board-wide ranked cost view (FLUX-1512).

**Safety rails.** A skip-permissions dispatched session can no longer auto-advance a ticket past Ready without surfaced confirmation (FLUX-850), finish/merge can gate on your repo's own CI checks, stack-agnostically (FLUX-560), ticket writes gained compare-and-swap so stale editors can't silently clobber concurrent changes (FLUX-1550, FLUX-1534), the plan gate defaults to Auto → You on fresh boards (FLUX-1497), and a real end-to-end lifecycle integration test now exercises grooming → Ready (FLUX-1569). The desktop app also warns before closing while sessions are still running (FLUX-1541).

**Chat & ticket UX.** Edit-and-resend / retry a chat turn (FLUX-685, FLUX-1566), fixes for vanishing or double-dispatched chat messages (FLUX-1493, FLUX-1494, FLUX-1495), the board orchestrator's flat 3s spawn tax removed (FLUX-1496), per-field dirty/instant-save editing on the ticket sideview (FLUX-979, FLUX-1568), and the sideview's section stack replaced by a tab bar with availability badges (FLUX-1515).

### Tickets

- **FLUX-1383**: Batch grooming: one session grooms N sibling tickets in one sitting
- **FLUX-1431**: configurable furnace watchdog and new default
- **FLUX-1441**: Verify streaming live-node markdown re-parse perf on long/marked-up responses
- **FLUX-1445**: Multi-workspace S0: memory hygiene — bound surfacedFinalMessages + copy-on-seed CONFIG_DEFAULTS
- **FLUX-1446**: Multi-workspace S1: Workspace registry core (keystone)
- **FLUX-1447**: Multi-workspace S2: routing contract + task-store.ts migration (template)
- **FLUX-1448**: Multi-workspace S3: routing migration — mcp-server.ts + HTTP routes
- **FLUX-1449**: Multi-workspace S4: routing migration — background job consumers
- **FLUX-1450**: Multi-workspace S5: SSE per-workspace filtering
- **FLUX-1451**: Multi-workspace S6: ID-collision-safe ticket write locks
- **FLUX-1452**: Multi-workspace S7: engine-lifetime timers iterate all live workspaces
- **FLUX-1453**: Multi-workspace S8: sync worker → per-workspace
- **FLUX-1454**: Multi-workspace S9: portal — board/workspace-id state dimension
- **FLUX-1455**: Multi-workspace S10: portal — non-destructive workspace switcher (tabs/board list)
- **FLUX-1462**: Per-phase tool tailoring for dispatched solo leads — trim each phase's lead toolset deliberately
- **FLUX-1479**: Scratchpad persona, phase handoff & per-phase model override (FLUX-1226 Phases D-F)
- **FLUX-1481**: make a plan file so i can test the anotation feature
- **FLUX-1486**: Pre-spawn session-launch failures are invisible while the portal window is focused
- **FLUX-1488**: Sequential order-rail drag handle missing touch-none (touch-drag reorder degraded)
- **FLUX-1489**: Unit-test the shared approve-and-start dispatch (dispatchApprovedImplementation)
- **FLUX-1490**: Finish req.workspace consumption in the S2-migrated HTTP handlers (read side)
- **FLUX-1491**: Re-seed adapter-boundary allowlist for build-initial-prompt.test.ts (4→6)
- **FLUX-1492**: config.json last-writer-wins: whole-file saveConfig from stale in-memory copies reverts settings (gate "reset" to Auto)
- **FLUX-1493**: Stale parked chat-queue message re-dispatched verbatim on chat reopen (zombie queue entry)
- **FLUX-1494**: Board /input mid-turn 409 guard is TOCTOU — two concurrent claude --resume spawned on one session
- **FLUX-1495**: Just-sent chat message invisible after minimize→reopen (user turn appended only after slow pre-spawn awaits)
- **FLUX-1496**: Board orchestrator spawns pay a flat 3s stdin-wait tax every turn (FLUX-1444 missed the board spec)
- **FLUX-1497**: Default the plan (grooming) gate to Auto → You for never-configured boards
- **FLUX-1498**: Multi-workspace: thread req.workspace into the unmigrated upsertManagedTicket write in pr-ticket.ts
- **FLUX-1502**: Communication-style contract: concise, reader-first output for personas + skill modules
- **FLUX-1503**: Deck member lines: live burn/temper state for tickets folded under PR and epic cards
- **FLUX-1505**: Commit-first data: optimistic button actions, patch-first SSE, scoped busy states, decision collapse
- **FLUX-1506**: Launch & loading feel: ghost session placeholder, skeleton system over spinners
- **FLUX-1507**: Motion continuity: shared physics tokens, view crossfades, card→modal morph, card flight
- **FLUX-1508**: Multi-workspace: route component-level board-scoped fetches through ehFetch
- **FLUX-1509**: '...more' in chat attributes shows the popup screen in wrong location and cut off
- **FLUX-1512**: Token observability panel: surface agent-payload metrics + waste signals in the portal
- **FLUX-1513**: Multi-workspace: tag Furnace batches with their owning workspace (S7 follow-up)
- **FLUX-1514**: Multi-workspace: thread ws into reclaimReadyWorktrees + recheckDependentBranches (S7 tail)
- **FLUX-1515**: Ticket sideview: replace the vertical section stack with a tab bar carrying availability badges
- **FLUX-1516**: Epic: Apple-feel responsiveness wave 2 — ten polish rooms from the audit
- **FLUX-1517**: Hover-intent prefetch: fetch ticket bodies on card hover so the modal opens warm
- **FLUX-1518**: Depth stack: board recedes behind TaskModal (iOS sheet grammar)
- **FLUX-1519**: Cold-boot cascade: staggered column/card reveal replaces the board's first-load pop
- **FLUX-1520**: Animated counters: column counts and badges roll instead of snapping
- **FLUX-1522**: Anchored notifications: ticket toasts fly from and dismiss into their card
- **FLUX-1523**: Interruptible motion: retargetable springs, no animation-completion gates
- **FLUX-1524**: Per-view continuity: preserve scroll and selection when leaving Backlog and Changes
- **FLUX-1525**: Drag lift physics: pickup scale/shadow/velocity tilt, spring settle on drop
- **FLUX-1526**: One success choreography: single confirm gesture, confetti reserved for releases
- **FLUX-1527**: Multi-workspace: unit-test batchBelongsToWorkspaceRoot + workspace-filtered batch accessors
- **FLUX-1528**: Ghost-launch failed-dissolve timer can wipe a live session on fast retry
- **FLUX-1529**: Multi-workspace S11: openWorkspace() bootstrap — load a board live without touching the active singleton
- **FLUX-1530**: Multi-workspace S12: per-request HTTP + SSE workspace routing (consume X-EH-Workspace / ?ws=)
- **FLUX-1531**: Multi-workspace S13: tag CLI sessions with their owning workspace + per-board count/stop
- **FLUX-1532**: Member lines & Agents pill: title-first text, phase-aware state label, staleness handling
- **FLUX-1533**: dragging into furnace is difficult drop target too small
- **FLUX-1534**: patchConfig: unlocked read-modify-write can clobber concurrent in-process patches
- **FLUX-1535**: Column.renderTask: dedupe the linked-PR/EpicDeck inner markup across the motion.div and plain-div branches
- **FLUX-1536**: useCardFlight: rapid re-trigger stacks flying clones instead of retargeting
- **FLUX-1537**: Notification dismiss-during-entrance flies an invisible clone
- **FLUX-1539**: show on card an icon coresponding with furnace batch it belongs to and give it a border color
- **FLUX-1540**: takes a whole minute for EH to start and it feels stuck
- **FLUX-1541**: too easy to close the app (electron) while something is still running
- **FLUX-1542**: Perf pass: fix motion-batch re-render amplification (hidden-view churn, taskUpdated fan-out, paint costs)
- **FLUX-1543**: bootProgress monotonic clamp shows stale count across workspace switch
- **FLUX-1547**: Boot hydration overhaul: parallel scan + persistent index + two-phase load (2000 tickets ≈ 60s → target <5s cold, instant warm) + smooth progress count
- **FLUX-1548**: Background loops (Temper tick, PR reconcile, gate runner) + engine self-dispatch resolve the WRONG workspace when a second board is open
- **FLUX-1549**: dragging a card now exerts huge lag due to furnace drop window squishes entire board
- **FLUX-1550**: Ticket body/metadata writes are last-writer-wins — stale whole-body PUT silently clobbers concurrent edits (no CAS)
- **FLUX-1551**: Multi-workspace follow-up (post-FLUX-1548): workspace-narrow session adoption, per-root worktree slot accounting, config-resolution tests
- **FLUX-1552**: Idle board burns ~24 main-thread frames/sec — convert remaining box-shadow keyframe loops to compositor-only recipe
- **FLUX-1553**: Interaction-burst render costs: cluster-card full-ticket sort in useMemo, token pill and icon render overhead
- **FLUX-1554**: Furnace multi-workspace hardening: thread batch.workspaceRoot through ignite/trigger/park/gh + fix furnace-store split-brain + MCP ownership checks
- **FLUX-1555**: Attention surfaces are process-global / active-board-targeted: notifications store+broadcasts, durable HITL prompt index, timed-out prompts vanish on non-active boards
- **FLUX-1556**: syncActiveSessionStubs writes all boards' stubs into the active board's store — restart reclaim guard then frees live sessions' worktrees
- **FLUX-1557**: Workspace activeKey semantics: unbound fallback follows last-OPENED board, tab-viewing never moves it, three disagreeing definitions of "active"
- **FLUX-1558**: Group context resolves via process-global singletons, not the bound workspace (get_project_group / group_doc fan-out)
- **FLUX-1559**: Non-orphan (.flux) mode has no local-file gitignore protection — config.json / read-state.json / boot-index.json churn the user's working tree
- **FLUX-1560**: agent-session-glow ring has square corners over the rounded-xl sessions button
- **FLUX-1561**: Cold-boot column entrance can freeze Ready/Done semi-transparent
- **FLUX-1562**: Harden disallowedEhToolsForDispatchedLead gate against an explicit 'standalone' patternPosition
- **FLUX-1563**: Agent-crash needsAction + notification from unbound exit handlers misroutes on non-active boards
- **FLUX-1564**: CI gate `checkCommand` runs against the main checkout, not the PR branch's tree
- **FLUX-1565**: Migrate REST group routes (routes/group.ts, routes/docs.ts, group-member-worktree.ts) to bound-workspace group resolution
- **FLUX-1566**: Edit/retry polish: modal Retry ignores composer chips; foreign-turn edge cases
- **FLUX-1567**: Furnace REST single-batch routes lack the cross-board ownership guard the MCP surface got (FLUX-1554 follow-up)
- **FLUX-1568**: Test coverage + polish for FLUX-979 per-field dirty/instant-save/CAS logic
- **FLUX-1569**: can we come up with a test suite that does a real integration test checking all the stages of a ticket from grooming into ready (without pr i guess)
- **FLUX-1570**: trying to drag ticket into the furnace hover sends it into done column instead
- **FLUX-1571**: Workspace header resolution misses when registry path ≠ canonical live key (8.3/casing) — echoed path silently serves the default board
- **FLUX-1572**: Second engine instance on the same workspace poisons .mcp.json (installer port rewrite) and falsely abandons the sibling engine's live sessions
- **FLUX-1580**: Persistent per-workspace orchestrator chat instances (switch, don't reset)
- **FLUX-560**: PR CI gating — gate finish/merge on the user's checks (stack-agnostic)
- **FLUX-685**: Edit-and-resend / retry a chat turn
- **FLUX-850**: Hard-gate: a skip-permission dispatched session cannot auto-advance a ticket past Ready without surfaced confirmation
- **FLUX-979**: editing ticket properties on ticket chat being dirty is unclear
- **PR-590**: PR #590: Multi-workspace S0: memory hygiene — bound surfacedFinalMessages + copy-on-seed CONFIG_DEFAULTS (+FLUX-1446, FLUX-1447)
- **PR-600**: PR #600: Re-seed adapter-boundary allowlist for build-initial-prompt.test.ts (4→6)
- **PR-601**: PR #601: Default the plan (grooming) gate to Auto → You for never-configured boards
- **PR-602**: PR #602: Multi-workspace S3: routing migration — mcp-server.ts + HTTP routes (+FLUX-1449, FLUX-1450, FLUX-1451, FLUX-1452, FLUX-1453, FLUX-1454, PR-602)
- **PR-603**: PR #603: Board /input mid-turn 409 guard is TOCTOU — two concurrent claude --resume spawned on one session (+FLUX-1493, FLUX-1495, FLUX-1496)
- **PR-604**: PR #604: Sequential order-rail drag handle missing touch-none (touch-drag reorder degraded)
- **PR-605**: PR #605: Unit-test the shared approve-and-start dispatch (dispatchApprovedImplementation)
- **PR-606**: PR #606: Pre-spawn session-launch failures are invisible while the portal window is focused
- **PR-607**: PR #607: Scratchpad persona, phase handoff & per-phase model override (FLUX-1226 Phases D-F)
- **PR-608**: PR #608: configurable furnace watchdog and new default
- **PR-609**: PR #609: Communication-style contract: concise, reader-first output for personas + skill modules
- **PR-610**: PR #610: Deck member lines: live burn/temper state for tickets folded under PR and epic cards
- **PR-611**: PR #611: Multi-workspace: thread req.workspace into the unmigrated upsertManagedTicket write in pr-ticket.ts
- **PR-612**: PR #612: Multi-workspace: tag Furnace batches with their owning workspace (S7 follow-up)
- **PR-613**: PR #613: '...more' in chat attributes shows the popup screen in wrong location and cut off
- **PR-614**: PR #614: Multi-workspace: thread ws into reclaimReadyWorktrees + recheckDependentBranches (S7 tail)
- **PR-615**: PR #615: Multi-workspace: route component-level board-scoped fetches through ehFetch
- **PR-616**: PR #616: Motion continuity: shared physics tokens, view crossfades, card→modal morph, card flight (+FLUX-1505, FLUX-1506)
- **PR-617**: PR #617: Ticket sideview: replace the vertical section stack with a tab bar carrying availability badges
- **PR-618**: PR #618: config.json last-writer-wins: whole-file saveConfig from stale in-memory copies reverts settings (gate "reset" to Auto)
- **PR-619**: PR #619: Depth stack: board recedes behind TaskModal (iOS sheet grammar)
- **PR-620**: PR #620: Multi-workspace S11: openWorkspace() bootstrap — load a board live without touching the active singleton
- **PR-621**: PR #621: Animated counters: column counts and badges roll instead of snapping
- **PR-622**: PR #622: Multi-workspace S12: per-request HTTP + SSE workspace routing (consume X-EH-Workspace / ?ws=)
- **PR-623**: PR #623: Hover-intent prefetch: fetch ticket bodies on card hover so the modal opens warm
- **PR-624**: PR #624: Multi-workspace S13: tag CLI sessions with their owning workspace + per-board count/stop
- **PR-625**: PR #625: Member lines & Agents pill: title-first text, phase-aware state label, staleness handling
- **PR-626**: PR #626: Anchored notifications: ticket toasts fly from and dismiss into their card
- **PR-627**: PR #627: Cold-boot cascade: staggered column/card reveal replaces the board's first-load pop
- **PR-628**: PR #628: Interruptible motion: retargetable springs, no animation-completion gates
- **PR-629**: PR #629: Drag lift physics: pickup scale/shadow/velocity tilt, spring settle on drop
- **PR-630**: PR #630: One success choreography: single confirm gesture, confetti reserved for releases
- **PR-631**: PR #631: Per-view continuity: preserve scroll and selection when leaving Backlog and Changes
- **PR-632**: PR #632: useCardFlight: rapid re-trigger stacks flying clones instead of retargeting
- **PR-633**: PR #633: dragging into furnace is difficult drop target too small
- **PR-634**: PR #634: Multi-workspace: unit-test batchBelongsToWorkspaceRoot + workspace-filtered batch accessors
- **PR-635**: PR #635: Ghost-launch failed-dissolve timer can wipe a live session on fast retry
- **PR-636**: PR #636: Multi-workspace S10: portal — non-destructive workspace switcher (tabs/board list)
- **PR-637**: PR #637: Column.renderTask: dedupe the linked-PR/EpicDeck inner markup across the motion.div and plain-div branches
- **PR-638**: PR #638: Notification dismiss-during-entrance flies an invisible clone
- **PR-639**: PR #639: patchConfig: unlocked read-modify-write can clobber concurrent in-process patches
- **PR-640**: PR #640: Perf pass: fix motion-batch re-render amplification (hidden-view churn, taskUpdated fan-out, paint costs)
- **PR-641**: PR #641: takes a whole minute for EH to start and it feels stuck
- **PR-643**: PR #643: show on card an icon coresponding with furnace batch it belongs to and give it a border color
- **PR-644**: PR #644: Finish req.workspace consumption in the S2-migrated HTTP handlers (read side)
- **PR-645**: PR #645: bootProgress monotonic clamp shows stale count across workspace switch
- **PR-646**: PR #646: dragging a card now exerts huge lag due to furnace drop window squishes entire board (+PR-646)
- **PR-647**: PR #647: Boot hydration overhaul: parallel scan + persistent index + two-phase load (2000 tickets ≈ 60s → target <5s cold, instant warm) + smooth progress count
- **PR-648**: PR #648: Background loops (Temper tick, PR reconcile, gate runner) + engine self-dispatch resolve the WRONG workspace when a second board is open
- **PR-649**: PR #649: Idle board burns ~24 main-thread frames/sec — convert remaining box-shadow keyframe loops to compositor-only recipe
- **PR-650**: PR #650: Ticket body/metadata writes are last-writer-wins — stale whole-body PUT silently clobbers concurrent edits (no CAS) (+PR-650)
- **PR-651**: PR #651: Interaction-burst render costs: cluster-card full-ticket sort in useMemo, token pill and icon render overhead
- **PR-652**: PR #652: Multi-workspace follow-up (post-FLUX-1548): workspace-narrow session adoption, per-root worktree slot accounting, config-resolution tests (+FLUX-1556, FLUX-1555, FLUX-1558, FLUX-1557, FLUX-1554)
- **PR-653**: PR #653: Fix cold-boot column entrance freezing Ready/Done semi-transparent (FLUX-1561) (+FLUX-1561)
- **PR-654**: PR #654: too easy to close the app (electron) while something is still running
- **PR-655**: PR #655: Non-orphan (.flux) mode has no local-file gitignore protection — config.json / read-state.json / boot-index.json churn the user's working tree
- **PR-656**: PR #656: agent-session-glow ring has square corners over the rounded-xl sessions button
- **PR-657**: PR #657: Per-phase tool tailoring for dispatched solo leads — trim each phase's lead toolset deliberately
- **PR-658**: PR #658: PR CI gating — gate finish/merge on the user's checks (stack-agnostic)
- **PR-659**: PR #659: Edit-and-resend / retry a chat turn
- **PR-660**: PR #660: Hard-gate: a skip-permission dispatched session cannot auto-advance a ticket past Ready without surfaced confirmation
- **PR-661**: PR #661: editing ticket properties on ticket chat being dirty is unclear
- **PR-662**: PR #662: CI gate `checkCommand` runs against the main checkout, not the PR branch's tree
- **PR-663**: PR #663: Edit/retry polish: modal Retry ignores composer chips; foreign-turn edge cases
- **PR-664**: PR #664: Migrate REST group routes (routes/group.ts, routes/docs.ts, group-member-worktree.ts) to bound-workspace group resolution (+FLUX-1563, FLUX-1571, FLUX-1567, FLUX-1572)
- **PR-665**: PR #665: Test coverage + polish for FLUX-979 per-field dirty/instant-save/CAS logic
- **PR-666**: PR #666: Harden disallowedEhToolsForDispatchedLead gate against an explicit 'standalone' patternPosition
- **PR-667**: PR #667: trying to drag ticket into the furnace hover sends it into done column instead
- **PR-668**: PR #668: can we come up with a test suite that does a real integration test checking all the stages of a ticket from grooming into ready (without pr i guess)
- **PR-669**: PR #669: Batch grooming: one session grooms N sibling tickets in one sitting
- **PR-670**: PR #670: Token observability panel: surface agent-payload metrics + waste signals in the portal
- **PR-671**: PR #671: Persistent per-workspace orchestrator chat instances (switch, don't reset)

